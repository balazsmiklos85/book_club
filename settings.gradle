rootProject.name = 'BookClub'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("libs.versions.toml"))
        }
    }
}

include 'adapter-web-thymeleaf'
include 'business'
include 'core'

